import React from 'react'

type NotesProps = {
    className?: string
}

const Notes:React.FC<NotesProps> = ({className=""}) => {
    return (
        <div>
            Notes
        </div>
    )
}

export default Notes;
